#include <iostream>

bool checkTuring(std::string);

int main() {
    std::cout << "Enter string:" << std::endl;
    std::string input;
    std::cin >> input;

    bool result = checkTuring(input);
    std::cout << result << std::endl;

    return 0;
}

bool checkTuring(std::string input) {
    int count1 = 0;
    int count0 = 0;

    while(count1 != 5) {

    }

    return true;
}
